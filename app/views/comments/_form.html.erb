<%= render 'shared/errors', resource: comment %>

<%= form_with(model: [forum_thread, comment], local: true) do |form| %>
  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
